{
    "$schema": "https://turbo.build/schema.json",
    "extends": [
        "//"
    ],
    "tasks": {
        "build": {
            "dependsOn": ["type-check", "lint" , "^build"],
            "inputs": ["**/*.ts", "**/*.js", "**/*.json", "**/*.scss", "**/*.tsx"],
            "outputs": ["dist/**"]
        }
    }
}